# QW-316 固件协作指南

## 项目概览

- 目标芯片：Nations N32G455CCL7（Cortex-M4）。
- 主工程：`MDK-ARM/Main.uvprojx`；默认 target 为 `N32G45x`，使用 Keil MDK-ARM 的 ARM Compiler 5，当前验证环境为 V5.06 update 6。工程元数据中的 `CLOCK` 为 12 MHz，但当前实际运行时钟为内部 8 MHz HSI 经 PLL 得到的 128 MHz。
- 同时保留 IAR 工程：`EWARM/ADC_SingleRead.ewp`。除非任务明确要求，不修改工程工具链、芯片型号或链接配置。
- 这是裸机协作式固件：SysTick 只递增 1 ms 时基；主循环在 `src/main.c` 中调度 10 ms、50 ms、100 ms 和 1000 ms 任务，完整执行一轮后喂独立看门狗，并在空闲时执行 `__WFI()`。

## 目录与职责

| 路径 | 内容 |
| --- | --- |
| `src/main.c` | 应用状态机、任务调度、按键/UI、电池与充电业务、安全停机入口。 |
| `src/n32g45x_it.c` | 中断服务程序与治疗双相波形时序；应保持短小、确定、无阻塞。 |
| `src/init.c` | 时钟、GPIO、ADC、DAC、定时器和 NVIC 初始化。 |
| `src/ble_protocol.c` | 蓝牙协议解析、CRC、应答、状态通知与超时处理。 |
| `inc/` | 应用与板级头文件、引脚/外设声明。 |
| `firmware/` | N32 CMSIS 与标准外设库；除芯片库升级或明确修复外不改动。 |
| `middlewares/rt-thread/`、`compoennts/` | 第三方中间件与 RTT 日志；避免无关格式化和大范围修改。 |
| `docs/` | 状态机、电池、UI、日志说明；行为变更后同步核对相关文档。 |
| `readme.txt` | 面向开发和联调人员的编译、架构、功能与重要宏概览。 |

`compoennts` 是仓库中的既有目录拼写，禁止为“修正拼写”而移动或批量重命名。

## 硬件与实时性约束

- 治疗输出涉及 TIM1、TIM8 与两路 DAC。`Treatment_StopOutputs()` 是统一安全关断路径；新增、修改状态切换、超时、故障或充电逻辑时，必须保证其等价安全关断仍会执行。
- `Pwr1/Pwr2` 被主循环和中断同时访问，保留 `volatile`；涉及多字节共享数据、读改写或跨中断状态时，使用已有临界区模式或极短的关中断保护，并说明原子性依据。
- 不在 ISR 或周期任务中加入 `Delay`、轮询等待、动态内存或阻塞 I/O。定时、去抖和超时应使用无符号 tick 差值，兼容 `uint32_t` 计数回绕。
- 修改 DAC、PWM、定时器、GPIO、电源或充电相关代码前，先确认原理图、N32G455 数据手册与目标引脚复用。无法确认时不要猜测寄存器含义或电气行为，应明确列出待确认项。
- DAC/PWM 的幅值、极性、死区和使能时序均属硬件安全边界。当前部分联调开关已按台架需要开启，不能据此视为量产配置；修改开关必须同步记录测试条件、验证结果和回退值。

## HV1.01 硬件引脚与板级配置

### 信息状态约定

- 本节中的“已确认”来自 HV1.01 板级连接或硬件说明；“源码现状”只描述当前参与 Keil 主工程编译的代码，不能替代原理图或实测。
- 标为“待确认”的电平、参数或器件不得作为量产固件依据。源码中已开启的联调输出只代表当前开发配置，不代表对应治疗高压、气泵或电磁阀已通过安全验收。
- 仓库中的 `w25q64`、彩屏、NFC 等未加入 `MDK-ARM/Main.uvprojx` 的旧驱动存在复用引脚冲突，不能纳入本板引脚基线。

### MCU、电源、时钟与调试

- MCU 已确认为 Nations N32G455CCL7，硬件版本为 HV1.01，VDD 为 3.3 V。
- 板上没有外部高速晶振。当前 `system_n32g45x.c` 使用内部 8 MHz HSI，经 PLL 得到 128 MHz SYSCLK；Keil 工程元数据中的 12 MHz `CLOCK` 不是当前运行时钟依据。
- VDDA 是 ADC、DAC、OPAMP、COMP 等模拟外设电源。N32G455 要求 VDDA 与 VDD 同电压，HV1.01 上的实际走线、滤波与去耦尚待原理图确认；N32G455CCL7 的 48 引脚封装没有独立 VREF+/VREF- 引脚，ADC/DAC 参考随 VDDA。
- PA6 上的外部参考采样信号不是 MCU 的 VREF 供电脚。当前源码按 2.5 V 精密参考参与电池换算，但参考器件型号和实际电压尚待确认。
- BOOT0 通过 10 kΩ 电阻下拉到 GND。NRST 通过 10 kΩ 上拉到 3.3 V，并通过 100 nF 电容接地。
- SWD/JTAG 相关信号引出到排母，具体排母针序待确认。当前源码关闭 JTAG 并保留 SWD，以释放 PB4；不得禁用 SWD 或改写调试脚配置，除非同时给出可靠恢复和量产下载方案。

### MCU 引脚分配

| MCU 引脚 | 板级网络/功能 | 当前配置与确认状态 |
| --- | --- | --- |
| PA2 | 压力传感器采样 | 已确认接 40 kPa GZP160 经约 47 倍放大后的模拟信号；源码由 ADC2 通道 11 独占采样，配置为模拟输入。 |
| PA3 | 电池电压采样 BATT | 源码为 ADC1 通道 4、模拟输入；源码按 1/2 分压计算，实际分压电阻待确认。 |
| PA4 | 原理图 `DAC2`、CH2 幅值控制 | 已确认 `DAC2 -> VP2 -> CH2`；对应 MCU `DAC_CHANNEL_1`。DAC 码值与 VP2/负载输出的方向、增益和零点待实测。 |
| PA5 | 原理图 `DAC1`、CH1 幅值控制 | 已确认 `DAC1 -> VP1 -> CH1`；对应 MCU `DAC_CHANNEL_2`。DAC 码值与 VP1/负载输出的方向、增益和零点待实测。 |
| PA6 | 外部参考采样 REF | 源码为 ADC1 通道 3、模拟输入，并假设输入为 2.5 V；硬件器件和标称值待确认。 |
| PA7 | IN2L | 已确认 CH2 左桥臂控制；当前使用 TIM8_CH1N（TIM8 不重映射），推挽复用输出，高电平有效。 |
| PA8 | IN1L | 已确认 CH1 左桥臂控制；当前复用为 TIM1_CH1，推挽复用输出，高电平有效。 |
| PA9 | IN1R | 已确认 CH1 右桥臂控制；当前复用为 TIM1_CH2，推挽复用输出，高电平有效。 |
| PA10 | PB_SS，启动/停止键 | 源码配置为内部下拉输入，按下按高电平处理；硬件外接电路待确认。 |
| PA11 | PB_PWRM，减键 | 源码配置为内部下拉输入，按下按高电平处理；硬件外接电路待确认。 |
| PA12 | SWEN，电磁阀控制 | 已确认高电平有效；高电平对应气囊放气还是封闭尚待确认。 |
| PB0 | IN2R | 已确认 CH2 右桥臂控制；当前使用 TIM8_CH2N（TIM8 不重映射），推挽复用输出，高电平有效。 |
| PB1 | BUZZ | 源码复用为 TIM3_CH4 蜂鸣器 PWM，输出极性高；蜂鸣器类型待确认。 |
| PB2 | BATEN | 源码按高电平开启电池采样分压及压力前端、低电平关闭；开机、工作和充电电池会话期间持续为高，关机或故障安全状态关闭，硬件有效电平仍待确认。 |
| PB4 | USART2_TX | 使用 USART2 第三重映射，推挽复用输出；当前串口为 115200、8N1、无流控。 |
| PB5 | USART2_RX | 使用 USART2 第三重映射，浮空输入；蓝牙模块型号及接口电平待确认。 |
| PB6 | BLEEN | 源码按低电平使能、高电平关闭；硬件有效电平待确认。 |
| PB9 | MOTOEN，气泵电机 | 已确认为气泵电机 PWM 控制，当前复用为 TIM4_CH4；气泵型号、额定电压/电流、驱动极性和允许 PWM 范围待确认。 |
| PB10 | CHARG | 充电状态输入，外部 10 kΩ 上拉至 3.3 V；源码按低电平表示充电中，是否直连 SLM6600 NCHRG 待确认。 |
| PB11 | STDBY | 充满状态输入，外部 10 kΩ 上拉至 3.3 V；源码按低电平表示充满，是否直连 SLM6600 NSTDBY 待确认。 |
| PB13 | PB_FUN，功能/模式键 | 源码配置为内部下拉输入，按下按高电平处理；硬件外接电路待确认。 |
| PB14 | PB_PWRP，加键 | 源码配置为内部下拉输入，按下按高电平处理；硬件外接电路待确认。 |
| PB15 | PB_MAIN，电源键 | 源码配置为内部下拉输入，按下按高电平处理；硬件外接电路待确认。 |
| PC13 | TM1621B_CS | LCD 段码驱动片选，普通推挽输出。 |
| PC14 | TM1621B_CLK | LCD 段码驱动时钟，普通推挽输出。 |
| PC15 | TM1621B_DATA | LCD 段码驱动数据，普通推挽输出；当前只按输出使用。 |
| PD0 | BLEN | 使用 PD0/PD1 重映射，源码按高电平开启 LCD 背光；硬件有效电平待确认。 |
| PD1 | VEN | 使用 PD0/PD1 重映射，普通推挽输出；控制对象和有效电平未知，默认保持低电平。 |

### 治疗输出拓扑与硬件安全边界

- CH1 已确认为 PA8/IN1L 与 PA9/IN1R，使用 TIM1_CH1/CH2；CH2 已确认为 PA7/IN2L 与 PB0/IN2R，使用 TIM8_CH1N/CH2N，TIM8 保持不重映射。每个通道通过四个三极管组成的桥式换向级，将 MCU 低压控制信号转换为高压双相刺激输出。
- 同一通道的 INxL 与 INxR 均为高电平有效，二者必须互斥，严禁同时导通；同时导通属于桥臂直通危险状态。两者均为低电平时已确认为该通道关闭状态。
- 任一极性切换必须执行“关闭当前桥臂 -> 全关断间隔 -> 开启另一桥臂”。全关断间隔的最小值尚无晶体管关断时间或示波器依据，不得擅自压缩；必须在 500 Ω 负载及最小/典型/最大档位下验证两路波形无交叠。
- 幅值链路已确认为 `PA5/DAC_CHANNEL_2/原理图 DAC1 -> VP1 -> CH1` 和 `PA4/DAC_CHANNEL_1/原理图 DAC2 -> VP2 -> CH2`。原理图网络名与 MCU DAC 通道号顺序相反，修改时不得仅凭 `DAC1/DAC2` 名称判断物理通道。
- 已知幅值控制级包含 LM321、三极管和高压调节器件，并由约 +53 V 高压电源形成 VP1/VP2；+53 V 容差、器件完整型号、DAC 单调方向、DAC=0/满量程时 VP 电压及 500 Ω 负载换算均待确认。
- 当前统一关断同时禁用 TIM1/TIM8 四路比较输出并将两路 DAC 写 0。桥臂双低已确认为关闭状态，但“DAC=0 即高压幅值安全归零”尚未通过硬件实测，必须作为台架验证项，不得仅凭代码判定安全。
- 当前 `TREATMENT_BRIDGE_PWM_OUTPUT_ENABLE=1`，TIM1/TIM8 的 CC3 比较中断用于结束 50 us 全关断死区；CC3 不驱动外部引脚。死区值仅为当前联调值，仍须用示波器确认换向无交叠。
- 当前治疗输出直接采用初始提交中已经由前同事实测的 TIM1/TIM8 配置、引脚通道用法和 DAC 包络代码，不再根据后续试验方案或 SDK 注释调整其时序与通道控制。现有统一安全关断继续保留；后续硬件复测仍应记录频率、脉宽、极性、换向空窗和负载幅值。
- 2026-09-09 台架反馈：CH2 改用 TIM8_CH1N/CH2N 后已观察到互补波形；测试条件和具体时序数值未记录。本次结果仅确认引脚能够输出，不代表 DAC 幅值、负载限压或换向空窗已经验收。
- 当前 P1/P2/P3 在软件中分别映射为原始代码的长/短梯形、长/短棱形、长/短三角包络组合。切换模式先统一关断并清零强度，再重置两路包络；通道强度从 0 增加到非零前重置该通道包络状态。

### 压力传感器、气泵与电磁阀

- 压力传感器已确认为 GZP160 系列、40 kPa 量程，经外部电路约 47 倍放大后输入 PA2。当前源码已有基于暂定零点、增益和灵敏度的 mmHg 换算、200 ms 实时采样、100 mmHg 测试设定点、150 mmHg 停泵上限，以及测试期间的平均值和最大值统计；这些常量尚未标定，不能作为医疗或量产依据。
- PB9/MOTOEN 控制充气电机，可由 TIM4_CH4 PWM 调速。目标是先以较低速度联调，避免充气过快损伤气囊；但过低占空比可能导致电机不启动或堵转发热，必须获得气泵型号、驱动电路和实测启动占空比后再确定参数。
- 当前 `TIM4_Configuration()` 使用预分频 799、周期 7999，源码注释为 100 Hz；按现有 128 MHz 时钟树推算可能约为 10 Hz，存在注释与配置不一致。未用示波器确认前，不得把任一数值写成气泵规格或量产参数。
- PA12/SWEN 控制电磁阀且高电平有效，但“有效”对应打开泄气通路还是关闭通路未知。当前压力页会实际驱动 TIM4_CH4 和 SWEN，只允许在受控台架条件下使用；确认失电状态和动作真值前不得据此判定气路安全。
- 预期气路状态为：充气时电机运行且气囊封闭；保持时电机停止且气囊封闭；放气时电机停止且气囊打开。电磁阀控制真值表未确认前，这只是控制目标而非 GPIO 真值表。
- 联调阶段最长连续充气暂定 60 s，定时放气暂定 3 s，正式测试最长 180 s；这些值不是量产定值。压力过大时必须立即停止充气；传感器断线、饱和、越界、ADC 超时或数据不可信时，必须立即停止气泵并进入经硬件确认的泄压状态。当前执行器已为联调开启，后续修改不得绕过超时与压力门禁。

### 充电、电池与状态检测

- 充电 IC 疑似 SLM6600，未核对芯片丝印和原理图前不得作为已确认型号。若确为 SLM6600，则 NCHRG/NSTDBY 为开漏低有效状态输出；PB10/PB11 外部各有 10 kΩ 上拉至 3.3 V。
- 当前源码假设：PB10/CHARG 低且 PB11/STDBY 高表示充电中，PB10 高且 PB11 低表示充满，两者均高表示未充电。实际 SLM6600 的无电池、欠压或温度故障也可能表现为两者均高，因此在缺少独立电源在位信号时，不得把双高唯一解释为“充电器已拔出”。
- SLM6600 的 TS 接法、PROG 电阻、实际充电电流、热设计以及电池包是否自带保护板尚待确认；软件状态显示不能替代过充、过流、过温和短路硬件保护。
- 电池标签记录为 `JS 903659 2500mAh 2024L12A50 3.7V 9.25Wh 82559`，按标称值视为单节锂电池。满充/截止电压、电芯供应商、NTC 和保护板规格待确认。

### 当前外设配置与待确认清单

- ADC1 当前为独立、软件触发、右对齐模式，在 PA3 电池与 PA6 外部参考之间切换；ADC2 独占 PA2 压力信号。ADC 时钟配置为 HCLK/16，采样时间为 239.5 周期；切换 ADC1 高阻信号源后应保留首样丢弃或足够建立时间。
- 两路 DAC 当前为 12 位右对齐、输出缓冲开启、选择 TIM6 TRGO 触发；当 `TREATMENT_DAC_OUTPUT_ENABLE=1` 时，`Board_Init()` 会显式初始化并启动 TIM6。修改 DAC 触发方式前应确认直写更新行为和输出实测，避免幅值保持旧值。
- USART2 当前通过 PB4/PB5 与蓝牙接口连接，参数为 115200、8 数据位、1 停止位、无校验、无硬件流控；蓝牙模块型号、使能时序、协议和电平尚待确认。
- 尚待补齐的关键硬件资料包括：完整原理图/网表，GZP160 完整尾码及放大电路，气泵和电磁阀型号，治疗桥晶体管型号，DAC-VP-负载标定，+53 V 高压源容差，VDDA/PA6 参考电路，SLM6600 丝印与外围参数，以及 SWD 排母针序。

### 当前关键编译开关（2026-09-12）

以下取值来自 `inc/main.h`，用于让协作者快速识别当前构建行为。源码变更后必须同步更新本节和 `readme.txt`。

| 宏 | 当前值 | 当前含义 |
| --- | ---: | --- |
| `TREATMENT_DAC_OUTPUT_ENABLE` | `1` | 启用两路 DAC 幅值链路和 TIM6。 |
| `TREATMENT_BRIDGE_PWM_OUTPUT_ENABLE` | `1` | 启用 TIM1/TIM8 桥臂 PWM 及 CC3 死区中断。 |
| `TREATMENT_BRIDGE_DEADTIME_US` | `50` | 桥臂换向全关断死区，限制范围为 13～499 us。 |
| `TREATMENT_DAC_FIXED_VALUE_TEST_ENABLE` | `0` | 固定 DAC 码值测试关闭；开启时使用 `TREATMENT_DAC_FIXED_VALUE`。 |
| `TREATMENT_DAC_FIXED_VALUE` | `2000` | 固定值联调码，编译检查上限为 3800。 |
| `PRESSURE_MAX_MMHG` | `150` | 最大压力停泵上限；测试中的被动压力达到该值不终止测试。 |
| `PRESSURE_TEST_SETPOINT_MMHG` | `100` | 充气设定压力，达到后停泵并等待压力变化。 |
| `PRESSURE_INFLATE_TIMEOUT_S` | `60` | 单次连续充气最长时间。 |
| `PRESSURE_TEST_TIMEOUT_S` | `180` | 正式测试最长时间。 |
| `PRESSURE_CHANGE_THRESHOLD_MMHG` | `1` | 相对设定点基准启动正式测试的最小压力变化。 |
| `BLE_REMOTE_POWER_OFF_CONTROL_ENABLE` | `1` | 允许远程关机事件。 |
| `BLE_REMOTE_TREATMENT_CONTROL_ENABLE` | `1` | 允许远程治疗危险事件进入状态机。 |
| `BLE_REMOTE_PRESSURE_CONTROL_ENABLE` | `1` | 允许远程压力危险事件进入状态机。 |
| `APP_IWDG_ENABLE` | `1` | 启用独立看门狗；仅在主循环完整返回后喂狗。 |
| `APP_IWDG_RELOAD_VALUE` | `2499` | LSI 40 kHz、32 分频时标称超时约 2 s。 |

蓝牙编译开关置 1 不会取消运行时门禁：危险动作仍必须通过应用状态、充电互锁和 PB7 物理连接检查，并通过现有事件队列执行。远程开机不支持，因为关机状态会关闭蓝牙模块。

## 代码修改规范

- 使用 C 语言与现有标准外设库 API；保持现有缩进、命名和最小改动范围，不引入动态分配或新的大型依赖。
- 为所有数组、环形队列、通信帧、ADC/DAC 数值和定时换算检查长度、范围、溢出、单位及错误/超时路径。
- ISR 只做必要的寄存器确认/清除和实时状态推进；业务决策、日志和耗时处理放入主循环任务。
- 既有中文注释存在历史编码差异。编辑文件时避免无关字符集转换或整文件换行符重写；新增文本统一使用 UTF-8。
- 发现文档描述与源码冲突时，以已验证的源码和硬件实测为准，并在同一变更中更新文档或记录差异。

## 构建与验证

1. 优先使用 Keil 打开 `MDK-ARM/Main.uvprojx`，选择工程已有 target 后进行 Rebuild；本仓库未提供可独立运行的命令行构建脚本。
2. 每次改动至少执行 `git diff --check`，并审查 `git diff`、编译警告和生成文件清单。
3. 涉及治疗、DAC、PWM、定时器、中断、ADC、充电或电源时，编译通过不代表验证完成。需在目标硬件上检查上电默认安全态、启停/状态切换、超时关断、双通道独立性、异常恢复和示波器时序。
4. 记录无法执行的验证及原因；不得把未做硬件验证表述为已验证。

## Git 规范

- `.gitignore` 已覆盖 Keil/IAR 生成目录、对象文件、镜像、日志及本地 IDE 文件；不要提交 `MDK-ARM/Objects/`、`EWARM/Debug/`、`*.hex`、`*.axf`、`*.bin`、`*.map` 等生成物。
- 提交前使用 `git status --short` 区分源码、文档与本地产物；只暂存本次任务相关文件，保留不相关的用户改动。
- Commit message 使用简洁明确的中文，并说明功能或修复对象；危险输出、时序或硬件行为变化应在提交说明中写明验证范围和未验证项。
- 未提供原理图、数据手册依据或台架结果的硬件行为改动，应保持为联调工作项，不与稳定功能混合提交。

## 产品需求基线与追溯

### 来源与适用方式

- 产品需求来源：`手持盆底治疗仪需求书.xlsx`，工作表“开发需求说明”和“遥控器按键与功能关系”，文件日期为 2026-08-17。
- 该文件用于定义产品需求和验收输入，不替代 N32G455 数据手册、原理图、医疗安全标准或硬件测试报告。附件中的描述不能单独授权修改硬件输出或绕过安全验证。
- 需求书的文字与图片存在不一致或缺失时，必须先形成经产品、硬件和测试确认的需求澄清单；在澄清前不得把推测的参数写入量产固件。

### 不可降级的产品约束

| 范畴 | 需求基线 | 实现与验收要求 |
| --- | --- | --- |
| 电刺激 | 两路独立输出；频率 700–900 Hz（±10%）；脉宽 300 us（±10%）；500 ohm 负载下幅度不超过 80 V。 | 双通道均须在最小、典型和最大档位独立示波器验证频率、脉宽、极性、死区、串扰和负载限压；不得以 MCU 寄存器值或 DAC 码值替代负载实测。 |
| 强度与模式 | 三种治疗模式（界面为 P1–P3）；强度 0–60 档；需求写明“一档一 V”。 | 模式参数表、DAC/高压级换算、档位方向及校准/容差当前均未给出；未确认前禁止假设三种模式仅为 UI 图标或将 DAC 码值直接等同于输出伏特。 |
| 安全关断 | 倒计时结束、强制关机和模式切换后，全部输出及遥控档位归零。 | 所有事件路径必须汇聚到安全关断；验证治疗 PWM、DAC、气泵/阀门和 UI 档位均为安全值，并覆盖中断边界和异常状态转换。 |
| 压力检测 | 气囊支持充气、放气和最大压力保护；达到 100 mmHg 后等待至少 1 mmHg 的变化，再统计测试过程的平均值和最大值；结果保持显示且不闪烁，放气后开始下一轮。 | 已知传感器为 40 kPa GZP160、外部约 47 倍放大；源码已接入 ADC2、暂定换算、60 s 充气超时、150 mmHg 停泵、180 s 测试超时和执行器控制，但完整型号、标定、失效检测、变化判据有效性及阀门真值仍未验收。 |
| 充电与电池 | 充电期间禁止使用功能；低电量闪烁；充电动画循环；充满后静止；要求过充保护。 | 软件应进入充电互锁状态并关闭危险输出。过充保护的责任边界（充电 IC 硬件或 MCU 控制）、充满信号极性和失效策略必须由原理图及硬件方案确认。 |
| 蓝牙/小程序 | 小程序与按键操作逻辑一致，支持主机遥控、数据传输和存储；未连接图标常亮、已连接图标闪烁。 | V1.1.2 帧格式、UI 动作、状态查询、停止、应答和断链关断已接入；鉴权、命令序号/幂等、数据持久化与隐私要求仍未定义。远程控制必须继续复用本地事件和安全检查。 |

### 人机交互基线

- 长按开/关机要求为 3 s；轻按电源键循环 10、20、30 min，默认 30 min。任何状态下调整时间是否立即影响正在运行的疗程，需要产品确认后固化为状态机规则。
- 模式键轻按在治疗与压力检测间切换，默认治疗模式；切换时强度必须清零。
- 启动/停止键在治疗模式下短按循环 P1–P3、长按选择通道；在压力模式下，空闲时短按启动充气、充气中再次短按停止、正式测试时短按结束测试，等待变化和结果保持阶段不得启动充气，长按 3 s 执行放气。气囊完成条件必须受最大压力和超时双重保护。
- 加/减键仅调整已选通道，范围为 0–60。需求书中“+ 键减小强度”的文字与“60 时该键无效”的描述冲突，默认不据此推断方向；应以确认后的交互定义和屏幕标识为准。
- LCD 至少应区分治疗页与检测页，显示时间、蓝牙、电池、治疗模式/通道/强度或压力值、充气/放气状态；闪烁对象与停止条件必须按需求书逐项验证。

### 当前源码追溯与实施门禁

- `src/n32g45x_it.c`、`src/init.c` 已恢复初始提交的 TIM1/TIM8 双路脉冲及 DAC 包络结构；当前周期 7999、比较值 7800 只是历史实现参数，必须以最终时钟树、引脚复用及示波器实测确认是否满足 800 Hz、300 us 目标与容差。
- `src/main.c` 已有治疗/压力/充电状态、两路强度字段、P1–P3 选择、10/20/30 min 时间循环、LCD 闪烁和统一 `Treatment_StopOutputs()`。这些软件框架不构成对幅值、无刺痛、双路无干扰或充电保护的验收结论。
- 当前电源及治疗页启动键长按常量为 2 s，压力页启动键长按为 3 s；压力流程已实现 60 s 充气、100 mmHg 设定点、1 mmHg 变化启动、180 s 测试统计、结果保持及放气复位。强度按 1 档递增/递减，范围 0～60。电源长按与需求书 3 s 要求仍有差异，真实输出也尚未完成“一档一 V”标定。
- 压力 ADC 协调采样、气泵/阀门控制和蓝牙 V1.1.2 接收解析已接入；压力传感器失效诊断、完整平均值业务、蓝牙鉴权/幂等及数据持久化仍为待实现项。
- 提交涉及上述需求的改动时，在提交说明或测试记录中列出：需求条目、静态检查/构建结果、台架条件（供电、负载、仪器）、实测结果、未覆盖场景和回退方式。
