# 盆底治疗仪蓝牙串口通讯协议 V1.1（草案）

## 1. 状态与适用范围

- 本文为重新建立的蓝牙串口应用层协议 V1.1，用于 MCU 与蓝牙模块透传串口之间的通信。
- V1.1 包含版本号、事务序号、统一应答、错误码、帧超时、链路心跳和危险输出互锁。
- 本文只定义通信行为，不能替代治疗幅值标定、500 ohm 负载测试、气泵/电磁阀真值表、压力上限和充电硬件保护。
- 危险输出包括治疗 PWM/DAC、高压链路、气泵和电磁阀。设备必须以本地状态机和硬件安全条件为最终裁决者，蓝牙命令不能直接写寄存器或 GPIO。

## 2. 链路参数与前置条件

| 项目 | 规定 |
| --- | --- |
| 串口 | USART2，115200 bit/s，8 数据位，1 停止位，无校验、无硬件流控 |
| MCU 引脚 | PB4 为 USART2_TX，PB5 为 USART2_RX；使用 USART2 第三重映射 |
| 模块使能 | PB6/BLEEN 的有效电平、上电稳定时间和连接状态引脚须以模块数据手册及原理图为准；未确认前不得假定 |
| 控制权 | 本协议不包含认证、鉴权或控制权申请流程；蓝牙链路建立后，主机发送的合法命令默认具有控制权 |
| 最大 DATA 长度 | 32 字节。`LEN > 32` 的帧直接丢弃并重新寻找帧头 |
| 帧间超时 | 接收首字节后 100 ms 内未收齐一帧，丢弃当前半帧并重新同步 |

> 注意：本协议默认已连接主机可控制设备。链路断开、心跳超时、充电互锁或设备故障时，设备仍必须按本协议进入相应安全状态。

## 3. 帧格式

```text
Byte 0..1  Byte 2  Byte 3  Byte 4  Byte 5..(4+LEN)  最后 1 字节
5A A5      VER     LEN     CMD     DATA[LEN]         CRC8
```

总帧长为 `6 + LEN` 字节；当 `LEN=0` 时不包含 DATA 字段。

| 字段 | 长度 | 说明 |
| --- | ---: | --- |
| FRAME | 2 | 固定为 `0x5A 0xA5` |
| VER | 1 | 固定为 `0x11`，表示 V1.1 |
| LEN | 1 | `DATA` 的字节数，范围 `0..32`；不包含 CMD、CRC 或其他字段 |
| CMD | 1 | 命令码 |
| DATA | N | 命令负载 |
| CRC8 | 1 | CRC-8/ATM |

### 3.1 字节序与 CRC

- 所有多字节整数均为**小端序**：先低字节、后高字节。
- CRC 参数：CRC-8/ATM，多项式 `0x07`、初值 `0x00`、不反射输入/输出、结果异或值 `0x00`。
- CRC 计算范围为从 `FRAME` 的第一个 `0x5A` 到 `DATA` 最后一个字节，包含帧头、`VER`、`LEN` 和 `CMD`，不包含 CRC 本身。
- 示例：空 `GET_STATUS` 请求的 CRC 前字节为 `5A A5 11 00 71`，CRC-8 的计算值为 `0x0F`；完整帧为：

```text
5A A5 11 00 71 0F
```

### 3.2 CRC-8/ATM 查表法

下表由本协议固定参数 `poly=0x07` 生成，索引为当前 CRC 与输入字节的异或值。初始 CRC 必须为 `0x00`。

```c
#include <stddef.h>
#include <stdint.h>

static const uint8_t s_protocol_crc8_table[256] =
{
    0x00U, 0x07U, 0x0EU, 0x09U, 0x1CU, 0x1BU, 0x12U, 0x15U, 0x38U, 0x3FU, 0x36U, 0x31U, 0x24U, 0x23U, 0x2AU, 0x2DU,
static uint8_t Protocol_Crc8(const uint8_t *data, uint16_t length)
{
    uint8_t crc = 0U;

    if ((data == NULL) && (length != 0U))
    {
        return 0U;
    }

    while (length != 0U)
    {
        crc = s_protocol_crc8_table[crc ^ *data];
        data++;
        length--;
    }

    return crc;
}
```

调用时的 `data` 指向帧首字节 `0x5A`，`length` 为 `5 + LEN`；返回值直接作为帧末尾 `CRC8`。`length=0` 时 CRC 结果为 `0x00`。

### 3.3 请求、响应和通知

- 协议采用串行请求/响应方式，同一时刻最多允许一条请求等待响应；主动通知通过独立命令码区分。
- 对 CRC 正确且命令格式合法的主机请求，设备必须在 200 ms 内返回一帧响应；执行异步动作时，先返回受理结果，再用状态通知反馈结果。
- 设备响应统一使用 `CMD = 0xE0`，避免“请求 CMD 加 0x80”与查询命令冲突。

```text
响应 DATA = REQ_CMD(1) + RESULT(1) + RESPONSE_DATA(N)
```

- 请求超时后允许重发完全相同的原帧，最多重试 2 次。所有设置和控制命令必须幂等，重复启动、充气或放气不得重新开始计时。
- CRC 错误、版本错误、长度错误的帧不响应；未知但格式正确的 `CMD` 应返回 `UNSUPPORTED`。

## 4. 结果码

| 值 | 名称 | 含义 |
| --- | --- | --- |
| `00` | OK | 命令已受理或查询成功 |
| `01` | BAD_LENGTH | DATA 长度不符合命令定义 |
| `02` | BAD_PARAMETER | 枚举值、范围或保留位非法 |
| `03` | UNSUPPORTED | 当前固件不支持该命令 |
| `04` | STATE_CONFLICT | 当前业务状态不允许该操作 |
| `05` | CHARGING_LOCK | 充电互锁，禁止执行危险操作 |
| `06` | SAFETY_LOCK | 压力、低电量、未标定、故障或其他安全条件不满足 |
| `07` | RESERVED | 保留，当前版本不得使用 |
| `08` | BUSY | 正在处理不可并行的动作 |
| `09` | INTERNAL_ERROR | 内部错误；设备同时应记录诊断信息 |

## 5. 命令表

| CMD | 方向 | 名称 | DATA | 说明 |
| --- | --- | --- | --- | --- |
| `01` | 主机→设备 | SET_TIME | `minutes:u8` | 设置疗程时长，仅允许 `10/20/30` 分钟 |
| `02` | 主机→设备 | SET_WORK_MODE | `mode:u8` | `0=治疗`，`1=压力检测` |
| `03` | 主机→设备 | SET_CH1_LEVEL | `level:u8` | CH1 强度 `0..60`，仅设置目标值，不启动输出 |
| `04` | 主机→设备 | SET_CH2_LEVEL | `level:u8` | CH2 强度 `0..60`，仅设置目标值，不启动输出 |
| `05` | 主机→设备 | SET_TREATMENT_PROFILE | `profile:u8` | `0=P1`，`1=P2`，`2=P3` |
| `06` | 主机→设备 | PRESSURE_INFLATE | 空 | 请求开始充气 |
| `07` | 主机→设备 | PRESSURE_DEFLATE | 空 | 请求开始放气 |
| `08` | 主机→设备 | STOP_ALL | 空 | 停止全部危险输出；任何状态均应允许 |
| `09` | 主机→设备 | START_THERAPY | 空 | 以已设置的参数请求开始治疗 |
| `71` | 主机→设备 | GET_STATUS | 空 | 查询完整状态快照 |
| `90` | 主机→设备 | LINK_KEEPALIVE | 空 | 链路心跳，每 1 s 发送一次 |
| `91` | 设备→主机 | STATUS_NOTIFY | 状态快照 | 状态变化或周期上报 |
| `92` | 设备→主机 | PRESSURE_NOTIFY | 压力数据 | 压力测量/状态变化上报 |
| `E0` | 设备→主机 | RESPONSE | 见 3.3 | 所有请求的统一应答 |

## 6. 负载定义和状态约束

### 6.1 SET_TIME / SET_WORK_MODE / SET_TREATMENT_PROFILE

- 仅在治疗输出和气路动作均已停止时允许修改；否则返回 `STATE_CONFLICT`。
- `SET_WORK_MODE` 成功时，设备必须保持治疗强度为 0，并通过统一安全关断路径关闭治疗输出。
- 任何未知枚举值必须返回 `BAD_PARAMETER`，不得按默认值执行。

### 6.2 SET_CH1_LEVEL / SET_CH2_LEVEL / START_THERAPY

- `level=0` 允许在任意非故障状态将相应通道目标强度清零。
- `level=1..60` 仅允许在治疗模式、未充电、无故障下设置；设置动作本身不得开启 PWM、DAC 或高压。
- `START_THERAPY` 还必须检查：至少一个通道强度非零、剩余时间非零、电池状态有效且允许工作、DAC/高压链路已完成有效标定、治疗输出未被安全锁定。
- 已处于治疗状态时，重复收到 `START_THERAPY` 应返回 `OK`，不得重置或延长疗程倒计时。
- 任一检查失败时，设备必须保证两路治疗输出均处于安全关断状态，并返回相应错误码。
- 强度档位不是输出伏特值。只有在每个通道完成 DAC—高压级—500 ohm 负载标定并验证不超过 80 V 后，才可将档位映射到实际治疗幅值。

### 6.3 PRESSURE_INFLATE / PRESSURE_DEFLATE

- 仅在压力检测模式、未充电、无故障下允许执行，二者互斥。
- 已处于对应气路动作时，重复收到相同命令应返回 `OK`，不得重新装载或延长动作超时。
- 充气前必须确认压力传感器数据有效且低于确认后的安全阈值；充气最长持续时间以设备本地硬件安全配置为准，当前联调上限为 3 s，不得由主机覆盖。
- 放气的 GPIO 电平及最大持续时间必须以气阀真值表和台架验证为准；未确认时返回 `SAFETY_LOCK`。
- 设备检测到传感器超量程、断线、采样超时、压力超限或任何故障时，必须立即停止气泵，并进入已由硬件确认的安全泄压状态。

### 6.4 STOP_ALL

- 设备收到 CRC 正确的 `STOP_ALL` 后，必须优先执行。
- 重复收到 `STOP_ALL` 时继续保持安全状态并返回 `OK`。
- 动作要求：治疗两通道目标强度清零、禁止治疗 PWM、DAC 写安全值、停止气泵，并将气阀置于经硬件确认的安全泄压状态；不得简单理解为关闭全部 GPIO。
- 应答中返回执行后的状态快照；如硬件状态无法确认，返回 `INTERNAL_ERROR` 并进入故障状态。

## 7. 状态快照与通知

`GET_STATUS` 的响应数据和 `STATUS_NOTIFY` 的 DATA 使用相同的 13 字节结构：

| 偏移 | 长度 | 字段 | 定义 |
| ---: | ---: | --- | --- |
| 0 | 1 | device_state | `0=关机`，`1=启动中`，`2=就绪`，`3=治疗`，`4=压力`，`5=充电`，`6=故障` |
| 1 | 1 | profile | `0=P1`，`1=P2`，`2=P3` |
| 2 | 1 | ch1_level | `0..60` |
| 3 | 1 | ch2_level | `0..60` |
| 4 | 1 | remaining_minutes | `0..30` |
| 5 | 1 | remaining_seconds | `0..59` |
| 6 | 1 | pressure_action | `0=空闲`，`1=充气`，`2=放气` |
| 7 | 2 | pressure_value | `u16` 小端序，单位 `1 mmHg/LSB`；`0xFFFF=无效`，仅当 flags 的 bit5=1 时有效 |
| 9 | 1 | battery_percent | `0..100`，`0xFF=无效` |
| 10 | 1 | flags | bit0=充电接入，bit1=充满，bit2=链路心跳有效，bit3=CH1输出，bit4=CH2输出，bit5=压力有效，bit6..7=保留0 |
| 11 | 2 | fault_code | 无故障为 `0x0000`，小端序 |

- 状态改变后 200 ms 内发送一次 `STATUS_NOTIFY`；稳定连接期间每 1 s 最多发送一次周期状态通知。
- `PRESSURE_NOTIFY` 的 DATA 为 `pressure_value:u16 + pressure_action:u8 + valid:u8 + fault_code:u16`，其中压力单位固定为 `1 mmHg/LSB`。数据无效时 `pressure_value=0xFFFF`、`valid=0`；通知频率不得超过 5 Hz。

### 7.1 故障码

| 值 | 含义 |
| --- | --- |
| `0000` | 无故障 |
| `0001` | 压力超限 |
| `0002` | 压力传感器或 ADC 数据无效 |
| `0003` | 充气超时 |
| `0004` | 电池数据无效 |
| `0005` | 低电量禁止工作 |
| `0006` | 治疗输出未标定或安全条件不满足 |
| `0007` | 内部状态异常 |

## 8. 连接、心跳、断链和本地优先级

1. 蓝牙模块建立链路后，主机无需认证、鉴权或申请控制权；设备直接接受 CRC、版本、长度和状态检查均通过的命令。
2. 主机每 1 s 发送一次空载荷 `LINK_KEEPALIVE`，设备收到有效心跳后刷新超时计时。
3. 远程启动治疗、充气或放气后启用心跳监控；连续 3 s 未收到有效心跳、蓝牙模块明确断链或设备进入故障时，设备必须停止远程启动的危险输出。未启动远程危险动作时，心跳超时不得影响本地操作。
4. 物理按键的关机/停止操作优先级最高；本地有效操作应向主机发送状态通知，避免双端状态不同步。
5. 充电状态下仅允许 `GET_STATUS`、`LINK_KEEPALIVE` 和 `STOP_ALL`；其他危险控制命令返回 `CHARGING_LOCK`。

## 9. 接收处理、资源与异常规则

- USART2 ISR 只读取 RX 数据寄存器并写入固定长度环形缓冲区；不做 CRC、日志、状态机操作、延时或阻塞发送。
- `Communication_Task10ms()` 负责帧头同步、100 ms 帧超时、版本/长度/CRC 检查、命令幂等处理、命令校验和事件投递。
- 环形缓冲区溢出时丢弃未完成帧、记录一次诊断计数；溢出期间不得执行任何未完整验证的命令。
- 所有主机可写数值均先检查长度、范围和当前状态，再转换为受限的应用事件；事件队列满时返回 `BUSY`，不得覆盖已有安全事件。
- 状态通知和响应发送应采用非阻塞发送队列；发送资源不足时优先保留 `RESPONSE` 和故障/安全状态通知，普通周期通知可以丢弃。

## 10. 实施与验收门禁

1. 先实现只读 `GET_STATUS`、`RESPONSE` 和接收容错测试，再实现 `STOP_ALL`。
2. 治疗强度、启动治疗、充气和放气命令默认编译关闭或返回 `SAFETY_LOCK`，直到硬件条件、标定表和台架验证已确认。
3. 必测：拆包/粘包、错误 CRC、异常 LEN、帧间超时、环形缓冲溢出、重复命令幂等、断链超时、充电互锁、故障期间命令、`STOP_ALL` 与中断边界竞争。
4. 对治疗输出必须在最小/典型/最大档位和 500 ohm 负载下实测频率、脉宽、死区、极性、双通道串扰和限压；对气路必须验证超压、ADC 失效、超时和泄压状态。
